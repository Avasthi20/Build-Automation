name: build flow

on: workflow_dispatch

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo 
      uses: actions/checkout@v6
    - name: listing files 
      run: ls 
  


